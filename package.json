{
  "name": "aquareum-monorepo",
  "private": true,
  "version": "0.0.1",
  "description": "The video layer for everything.",
  "main": "index.js",
  "scripts": {
    "check": "yarn workspaces foreach --all run check && git ls-files | xargs prettier --check --ignore-unknown",
    "fix": "git ls-files | xargs prettier --write --ignore-unknown",
    "prepare": "husky",
    "build": "yarn workspaces foreach --all run build"
  },
  "keywords": [],
  "author": "Aquareum",
  "license": "MIT",
  "devDependencies": {
    "husky": "^9.0.11",
    "lint-staged": "^15.2.7",
    "prettier": "^3.3.2"
  },
  "workspaces": [
    "js/*"
  ],
  "packageManager": "yarn@4.3.0",
  "dependencies": {
    "firebase-admin": "^12.2.0"
  }
}
